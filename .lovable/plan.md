Remover o bloco do card "Backup do Sistema" (linhas 515–531) em `src/pages/SuperAdminPanel.tsx`, incluindo o wrapper grid "Atalhos administrativos" que só contém ele. A rota `/admin/backup` e a página continuam existindo, apenas o atalho do dashboard é retirado. Se `HardDrive` ficar sem uso, removo o import também.